# v4.4.5 Discord per-browser hotfix

- Added a webhook field to Connections.
- Each browser stores its own Discord webhook in localStorage.
- Discord requests remain Firebase-authenticated and verify group membership.
- The Netlify environment webhook is retained only as an optional fallback.
- Test messages do not create activity-feed entries.
- Service-worker cache revision bumped so existing beta testers receive the new files.
