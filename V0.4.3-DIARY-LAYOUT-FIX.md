# v0.4.3 Diary Layout Fix

Fixes:
- Tier cards now fill the available region width.
- Easy/Medium/Hard/Elite display in a responsive 2x2 grid.
- Opening a tier expands it across the full row.
- Removes the large unused empty area inside each region.
- Mobile uses a clean one-column layout.
