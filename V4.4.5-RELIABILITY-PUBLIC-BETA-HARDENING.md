# v4.4.5 Reliability & Public Beta Hardening

- Correct service-worker navigation fallback; failed images/tiles no longer receive index.html.
- Centralized visible build version and updated package/cache metadata.
- Added resilient Leaflet CDN redundancy and graceful map-library failure messaging.
- Added image fallback behavior for unavailable external item sprites.
- Restored visible cloud-sync status in Settings.
- Replaced invite-management blocking alerts with in-app toasts.
- Added Firestore-backed rate limiting to sensitive Netlify endpoints.
- Added progress-document shape, key, and map-size validation in Firestore rules.

Deploy the site/functions first, then publish the included firestore.rules.
